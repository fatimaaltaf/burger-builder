import React from "react";
import Logo from "../../Logo/Logo";
import "./Toolbar.css";

const Toolbar = props => (
  <header className="Toolbar">
    <div>Menu</div>
    <div>
      <Logo />
    </div>
    <nav>...</nav>
  </header>
);

export default Toolbar;
